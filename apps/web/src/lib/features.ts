/**
 * Feature flags — flip to re-enable temporarily-hidden features.
 *
 * CREDITS_ENABLED = false → hides all AI-credits UI (balance, top-up, trial,
 * ad revenue, referrals, offers, low-credit banners).  Only the monthly
 * subscription flow remains active.  Set to true to restore the credits system.
 */
export const CREDITS_ENABLED = false;
