[build]
buildCommand = "pnpm install --no-frozen-lockfile && pnpm --filter @cf/shared build && pnpm --filter @cf/api build"

[deploy]
startCommand = "node /app/apps/api/dist/main"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
