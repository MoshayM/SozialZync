name: CI

on:
  push:
    branches: [master, main, develop]
  pull_request:
    branches: [master, main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

# pnpm version comes from package.json's packageManager field (pnpm@11.x) —
# pnpm/action-setup reads it; pinning it here too made the action error out.
# Node 24 to match local dev; pnpm 11.x requires Node >= 22.13.
env:
  NODE_VERSION: '24'

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build shared packages
        run: pnpm --filter @cf/tsconfig --filter @cf/eslint-config --filter @cf/shared build

      - name: Lint
        run: pnpm lint

  typecheck:
    name: Typecheck
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      # @prisma/client's postinstall only writes a stub — the real client
      # (UserRole, PricingRule, …) comes from generating against the schema.
      - name: Generate Prisma client
        run: pnpm --filter @cf/api exec prisma generate

      - name: Build shared packages
        run: pnpm --filter @cf/tsconfig --filter @cf/shared build

      - name: Typecheck
        run: pnpm typecheck

  unit-tests:
    name: Unit Tests
    runs-on: ubuntu-latest
    env:
      NODE_ENV: test
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Generate Prisma client
        run: pnpm --filter @cf/api exec prisma generate

      - name: Build shared packages
        run: pnpm --filter @cf/tsconfig --filter @cf/shared build

      # No "--" separator: pnpm 11 forwards it literally, and jest then reads
      # "--coverage --ci" as a test-name pattern (matching zero tests).
      - name: Run unit tests with coverage
        run: pnpm --filter @cf/api run test --coverage --ci

      - name: Upload coverage report
        uses: actions/upload-artifact@v7
        if: always()
        with:
          name: coverage-report
          path: apps/api/coverage/
          retention-days: 7

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v4
        if: always()
        with:
          directory: apps/api/coverage/
          flags: unit
          fail_ci_if_error: false

  build:
    name: Build
    runs-on: ubuntu-latest
    needs: [lint, typecheck, unit-tests]
    env:
      SKIP_ENV_VALIDATION: 'true'
      NEXT_TELEMETRY_DISABLED: '1'
      # Stub values so Next.js build doesn't fail on missing env vars
      NEXT_PUBLIC_API_URL: 'http://localhost:4001/api/v1'
      NEXT_PUBLIC_USE_MOCK: 'false'
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Generate Prisma client
        run: pnpm --filter @cf/api exec prisma generate

      - name: Build all packages and apps
        run: pnpm build
        env:
          SENTRY_AUTH_TOKEN: ${{ secrets.SENTRY_AUTH_TOKEN }}

      # Performance budget gate (docs4/44): per-route first-load JS ≤ 800 KB
      # raw, total unique JS ≤ 1500 KB. Baseline 2026-07: 571 KB / 1001 KB.
      # Also diffs against scripts/bundle-budget-baseline.json (trend WARNs).
      - name: Check bundle budget
        run: node scripts/check-bundle-budget.mjs
        working-directory: apps/web

      # Trend artifact (docs4/44): per-route sizes for this run, kept 90 days
      # so budget creep is inspectable across builds, not just pass/fail.
      - name: Upload bundle budget report
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: bundle-budget-report
          path: apps/web/.next/bundle-budget-report.json
          if-no-files-found: ignore

      - name: Upload Next.js standalone artifact
        uses: actions/upload-artifact@v7
        with:
          name: nextjs-standalone
          path: |
            apps/web/.next/standalone/
            apps/web/.next/static/
            apps/web/public/
          retention-days: 1

      - name: Create Sentry release
        if: github.ref == 'refs/heads/master' && env.SENTRY_AUTH_TOKEN != ''
        env:
          SENTRY_AUTH_TOKEN: ${{ secrets.SENTRY_AUTH_TOKEN }}
          SENTRY_ORG: ${{ secrets.SENTRY_ORG }}
          SENTRY_PROJECT: ${{ secrets.SENTRY_PROJECT }}
        run: |
          npx @sentry/cli releases new ${{ github.sha }}
          npx @sentry/cli releases set-commits ${{ github.sha }} --auto
          npx @sentry/cli releases finalize ${{ github.sha }}
          npx @sentry/cli releases deploys ${{ github.sha }} new -e production

  security:
    name: Security Audit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Audit dependencies for high/critical CVEs
        run: pnpm audit --audit-level=high
        continue-on-error: true

      - name: Dependency review (PR only)
        if: github.event_name == 'pull_request'
        uses: actions/dependency-review-action@v4
        with:
          fail-on-severity: high

  semgrep:
    name: Semgrep SAST
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # Gate: custom architecture rules at ERROR severity must pass.
      # No --quiet on the scan: it swallowed the reason for config errors
      # (the job failed with a bare exit 7 and nothing in the log).
      - name: Run custom rules (blocking)
        run: |
          pip install semgrep --quiet
          semgrep --version
          semgrep scan \
            --config .semgrep/ \
            --error \
            --severity ERROR
        env:
          SEMGREP_APP_TOKEN: ${{ secrets.SEMGREP_APP_TOKEN }}

      # Informational: TypeScript registry pack — findings don't block the build.
      - name: Run registry pack (informational)
        run: |
          semgrep scan \
            --config p/typescript \
            --quiet
        continue-on-error: true
        env:
          SEMGREP_APP_TOKEN: ${{ secrets.SEMGREP_APP_TOKEN }}

  # DAST (docs4/23): passive + active scan of the production web build on every
  # PR/push. Reuses the pre-built standalone artifact from the build job so no
  # rebuild is needed. High-risk findings from the passive scan fail the job;
  # the active scan runs in continue-on-error mode (informational).
  zap-security:
    name: ZAP Security Scan (DAST)
    runs-on: ubuntu-latest
    timeout-minutes: 30
    needs: [build]
    env:
      SKIP_ENV_VALIDATION: 'true'
      NEXT_TELEMETRY_DISABLED: '1'
      NEXT_PUBLIC_API_URL: 'http://localhost:4007/api/v1'
      NEXT_PUBLIC_USE_MOCK: 'false'
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm

      - name: Install dependencies (no build — reusing artifact)
        run: pnpm install --frozen-lockfile

      - name: Download pre-built Next.js standalone
        uses: actions/download-artifact@v4
        with:
          name: nextjs-standalone
          path: apps/web/.next/standalone

      - name: Copy static assets into standalone
        run: |
          cp -r apps/web/.next/static apps/web/.next/standalone/.next/static
          cp -r apps/web/public apps/web/.next/standalone/public

      - name: Start web app from pre-built artifact
        run: |
          node apps/web/.next/standalone/server.js &
          for i in $(seq 1 30); do
            if curl -sf http://localhost:3007/ > /dev/null 2>&1; then
              echo "Web is up"; break
            fi
            echo "Waiting for web... ($i/30)"
            sleep 1
          done
        env:
          PORT: 3007
          HOSTNAME: '0.0.0.0'

      - name: Run ZAP passive baseline scan
        run: |
          chmod a+w .zap
          docker run --network host -v "$PWD/.zap:/zap/wrk" \
            ghcr.io/zaproxy/zaproxy:stable \
            zap.sh -cmd -silent -autorun /zap/wrk/plan.yaml

      - name: Gate on High-risk findings (passive)
        run: node .zap/check-zap-summary.mjs .zap/zap-baseline-web.json

      - name: Run ZAP active scan
        # Active scan attempts real attacks — runs after passive to extend findings.
        # Uses a separate plan to avoid overwriting the passive report.
        run: |
          chmod a+w .zap
          docker run --network host -v "$PWD/.zap:/zap/wrk" \
            ghcr.io/zaproxy/zaproxy:stable \
            zap.sh -cmd -silent -autorun /zap/wrk/plan-active.yaml
        continue-on-error: true

      - name: Upload ZAP reports
        uses: actions/upload-artifact@v7
        if: always()
        with:
          name: zap-security-report
          path: |
            .zap/zap-baseline-web.md
            .zap/zap-baseline-web.json
            .zap/zap-active-web.md
            .zap/zap-active-web.json
            .zap/zap-summary.json
          retention-days: 30

  trufflehog:
    name: Secret Scan (TruffleHog)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: TruffleHog OSS
        uses: trufflesecurity/trufflehog@main
        with:
          path: ./
          base: ${{ github.event.repository.default_branch }}
          head: HEAD
          extra_args: --debug --only-verified

  owasp-dep-check:
    name: OWASP Dependency Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: OWASP Dependency Check
        uses: dependency-check/Dependency-Check_Action@main
        with:
          project: 'creatorforce-ai'
          path: '.'
          format: 'HTML'
          out: 'reports'
          args: >
            --enableRetired
            --failOnCVSS 9
        continue-on-error: true
      - name: Upload OWASP report
        uses: actions/upload-artifact@v7
        if: always()
        with:
          name: owasp-dep-check-report
          path: reports/
          retention-days: 30

  e2e:
    name: E2E Tests (Playwright, ${{ matrix.browser }})
    runs-on: ubuntu-latest
    # 25 wasn't enough once the suite actually ran (jobs were cancelled at the
    # cap on the first real run) — give headroom while the suite stabilizes.
    timeout-minutes: 40
    needs: [lint, typecheck, unit-tests]
    # Cross-browser sharding (docs4/22): one job per browser keeps each run
    # inside the timeout instead of tripling a single job.
    strategy:
      fail-fast: false
      matrix:
        browser: [chromium, firefox, webkit]

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: cf
          POSTGRES_PASSWORD: cf
          POSTGRES_DB: creatorforce
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

      redis:
        image: redis:7
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    env:
      DATABASE_URL: postgresql://cf:cf@localhost:5432/creatorforce
      # BullModule is hardcoded to 127.0.0.1:6379 in app.module.ts; service maps to that.
      REDIS_URL: redis://localhost:6379
      JWT_SECRET: ci-secret-e2e
      # OAuth token encryption (auth wave) — service refuses to boot under 32 chars.
      TOKEN_ENCRYPTION_KEY: ci-e2e-token-encryption-key-0123456789abcdef
      API_PORT: '4007'
      NODE_ENV: production
      # Silence optional external integrations so the API starts without real keys.
      SKIP_ENV_VALIDATION: 'true'
      ANTHROPIC_API_KEY: ci-placeholder
      OPENAI_API_KEY: ci-placeholder
      GEMINI_API_KEY: ci-placeholder

    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build shared packages
        run: pnpm --filter @cf/tsconfig --filter @cf/eslint-config --filter @cf/shared build

      - name: Generate Prisma client
        run: pnpm --filter @cf/api exec prisma generate
        env:
          DATABASE_URL: ${{ env.DATABASE_URL }}

      - name: Run database migrations
        run: pnpm --filter @cf/api exec prisma migrate deploy
        env:
          DATABASE_URL: ${{ env.DATABASE_URL }}

      - name: Build API
        run: pnpm --filter @cf/api build

      - name: Start API in background
        # Fail HARD when the API never comes up — a silently absent API
        # previously surfaced as inscrutable Playwright failures.
        run: |
          node apps/api/dist/main.js &
          up=0
          # /health is the VERSION_NEUTRAL liveness probe (docs4/39) — /api/docs
          # is dev-only and 404s under NODE_ENV=production, which made this
          # loop report a healthy API as dead.
          for i in $(seq 1 30); do
            if curl -sf http://localhost:4007/health > /dev/null 2>&1; then
              echo "API is up"; up=1; break
            fi
            echo "Waiting for API... ($i/30)"
            sleep 1
          done
          if [ "$up" != "1" ]; then echo "API failed to start"; exit 1; fi
        env:
          DATABASE_URL: ${{ env.DATABASE_URL }}
          JWT_SECRET: ${{ env.JWT_SECRET }}
          TOKEN_ENCRYPTION_KEY: ${{ env.TOKEN_ENCRYPTION_KEY }}
          API_PORT: ${{ env.API_PORT }}
          NODE_ENV: ${{ env.NODE_ENV }}
          ANTHROPIC_API_KEY: ${{ env.ANTHROPIC_API_KEY }}
          OPENAI_API_KEY: ${{ env.OPENAI_API_KEY }}
          GEMINI_API_KEY: ${{ env.GEMINI_API_KEY }}

      # Production build for the e2e web server (playwright.config uses
      # `next start` on CI). NEXT_PUBLIC_* are inlined at build time.
      - name: Build web app
        run: pnpm --filter @cf/web... build
        env:
          NEXT_PUBLIC_API_URL: 'http://localhost:4007/api/v1'
          NEXT_PUBLIC_USE_MOCK: 'false'
          SKIP_ENV_VALIDATION: 'true'
          NEXT_TELEMETRY_DISABLED: '1'

      - name: Install Playwright browsers
        run: pnpm --filter @cf/e2e exec playwright install --with-deps ${{ matrix.browser }}

      - name: Run Playwright E2E tests
        run: pnpm --filter @cf/e2e exec playwright test --project=${{ matrix.browser }}
        env:
          CI: 'true'
          # playwright.config.ts webServer block hardcodes NEXT_PUBLIC_API_URL=http://localhost:4007/api/v1
          # and starts the Next.js dev server via 'pnpm --filter @cf/web dev' automatically.
          # The job-level NODE_ENV=production (needed by the API) must not leak
          # into `next dev` — it makes Next look for a production build and die
          # with required-server-files.json ENOENT.
          NODE_ENV: development
          NEXT_PUBLIC_API_URL: 'http://localhost:4007/api/v1'
          NEXT_PUBLIC_USE_MOCK: 'false'
          SKIP_ENV_VALIDATION: 'true'

      - name: Upload Playwright report
        uses: actions/upload-artifact@v7
        if: failure()
        with:
          name: playwright-report-${{ matrix.browser }}
          path: apps/e2e/playwright-report/
          retention-days: 7
